[project]
name = "tulip-mcp"
version = "0.1.0"
description = "MCP for Tulip CTF platform"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.20.0",
]
